<!doctype html>
<html lang="pt-BR">
  <head>
    <meta charset="utf-8"/>
    <meta name="viewport" content="width=device-width, initial-scale=1, viewport-fit=cover"/>
    <title>MC Router Manager</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/@tabler/core@1.0.0-beta17/dist/css/tabler.min.css">
    <style>
      @import url('https://rsms.me/inter/inter.css');
      :root { 
          --tblr-font-sans-serif: 'Inter Var', -apple-system, sans-serif; 
          /* Cores baseadas no Nginx Proxy Manager */
          --npm-bg-dark: #1b1c21;
          --npm-panel-dark: #23242c;
          --npm-green: #81ff00; /* Verde vibrante do botão */
          --npm-green-hover: #72e000;
          --npm-text-grey: #babbbc;
      }
      
      body { 
          font-feature-settings: "cv03", "cv04", "cv11"; 
          background-color: var(--npm-bg-dark) !important;
          color: #f1f1f1;
      }

      /* Navbar Dark override */
      .navbar-dark {
          background-color: #16171d !important;
          border-bottom: 1px solid #2d2f36;
      }
      .navbar-brand {
          font-weight: 700;
          font-size: 1.2rem;
          color: #fff !important;
      }

      /* Card Style - Imitando painel do NPM */
      .card {
          background-color: var(--npm-panel-dark) !important;
          border: none;
          box-shadow: 0 4px 6px rgba(0,0,0,0.1);
      }
      .card-header {
          border-bottom: 1px solid #2d2f36;
          background-color: transparent;
      }
      .card-title {
          color: #fff;
          font-weight: 500;
      }

      /* Tabela Styles */
      .table-vcenter td, .table-vcenter th {
          border-bottom: 1px solid #2d2f36;
          color: var(--npm-text-grey);
      }
      .table thead th {
          background-color: transparent;
          color: #7a7b80;
          text-transform: uppercase;
          font-size: 0.75rem;
          font-weight: 600;
      }
      
      /* Botão Verde NPM */
      .btn-npm-primary {
          background-color: #8cc63f !important; /* Um verde um pouco mais sóbrio, ajustado para legibilidade */
          background-image: linear-gradient(180deg, #95d544, #81c035);
          color: #1a1a1a !important;
          font-weight: 600;
          border: none;
      }
      .btn-npm-primary:hover {
          filter: brightness(1.1);
      }

      /* Badge Online */
      .badge-online {
          background-color: rgba(129, 255, 0, 0.15);
          color: #95d544;
          border: 1px solid rgba(149, 213, 68, 0.3);
          padding: 4px 8px;
          border-radius: 4px;
          font-size: 11px;
          text-transform: uppercase;
          font-weight: 700;
      }
      .dot-online {
          display: inline-block;
          width: 8px;
          height: 8px;
          background-color: #95d544;
          border-radius: 50%;
          margin-right: 5px;
      }

      /* Inputs estilo dark */
      .form-control {
          background-color: #16171d;
          border: 1px solid #2d2f36;
          color: #fff;
      }
      .form-control:focus {
          background-color: #16171d;
          border-color: #4a4d55;
          color: #fff;
      }
      .modal-content {
          background-color: var(--npm-panel-dark);
          color: #fff;
      }
      .modal-header, .modal-footer {
          border-color: #2d2f36;
      }
      .form-label {
          color: var(--npm-text-grey);
      }
      .text-white-strong {
          color: #fff !important;
          font-weight: 500;
      }
      .chip-domain {
          background-color: #2d2f36;
          padding: 2px 8px;
          border-radius: 4px;
          color: #fff;
          font-family: monospace;
      }
    </style>
  </head>
  <body class="theme-dark">
    <div class="page">
      <header class="navbar navbar-expand-md navbar-dark d-print-none">
        <div class="container-xl">
          <div class="d-flex align-items-center">
            <svg xmlns="http://www.w3.org/2000/svg" class="icon icon-tabler icon-tabler-hexagon-3d me-2" width="30" height="30" viewBox="0 0 24 24" stroke-width="1.5" stroke="#ffffff" fill="none" stroke-linecap="round" stroke-linejoin="round">
              <path stroke="none" d="M0 0h24v24H0z" fill="none"/>
              <path d="M12 3l5 3v6l-5 3l-5 -3v-6z" />
              <path d="M12 3v6l5 3" />
              <path d="M12 9l-5 3" />
              <path d="M12 12l0 9" />
            </svg>
            <h1 class="navbar-brand navbar-brand-autodark d-none-navbar-horizontal pe-0 pe-md-3">
              <a href="/">MC Router Manager</a>
            </h1>
          </div>
          
          <div class="navbar-nav flex-row order-md-last">
            <div class="nav-item dropdown">
              <a href="#" class="nav-link d-flex lh-1 text-reset p-0" data-bs-toggle="dropdown" aria-label="Open user menu">
                <span class="avatar avatar-sm" style="background-image: url(https://ui-avatars.com/api/?name=<%= user.name %>&background=333&color=fff)"></span>
                <div class="d-none d-xl-block ps-2">
                  <div style="color: #fff;"><%= user.name %></div>
                  <div class="mt-1 small" style="color: #777;"><%= user.email %></div>
                </div>
              </a>
              <div class="dropdown-menu dropdown-menu-end dropdown-menu-arrow bg-dark border-secondary">
                <a href="/logout" class="dropdown-item text-white">Sair</a>
              </div>
            </div>
          </div>
        </div>
      </header>
      <div class="page-wrapper">